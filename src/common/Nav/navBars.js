import React from "react";
import AppBar from "./AppBar";
import Drawer from "./Drawer";

const NavBars = (props) => {
  return (
    <div>
      <AppBar />
      <Drawer />
    </div>
  );
};

export default NavBars;
